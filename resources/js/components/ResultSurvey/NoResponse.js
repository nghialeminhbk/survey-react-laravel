const NoResponse = () => {
    return ( 
        <div class="alert alert-body border fw-bold ms-3 mt-3">
            <p>This typeform doesn’t have any responses yet. If you haven’t set up notifications, you can do that now and we’ll message you when someone responds.</p>
        </div>
     );
}
 
export default NoResponse;